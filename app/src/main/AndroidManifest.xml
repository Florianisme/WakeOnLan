<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.florianisme.wakeonlan">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WakeOnLan">
        <activity
            android:name=".home.MainActivity"
            android:exported="true"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.WakeOnLan.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".home.addmachine.AddMachineActivity"
            android:label="@string/title_activity_add_machine" />
        <activity
            android:name=".home.addmachine.EditMachineActivity"
            android:label="@string/title_activity_edit_machine" />

        <service
            android:name=".quicksettings.MachineOneTileService"
            android:label="@string/tile_name_machine_one"
            android:icon="@drawable/ic_tile_icon"
            android:exported="true"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
        <service
            android:name=".quicksettings.MachineTwoTileService"
            android:label="@string/tile_name_machine_two"
            android:icon="@drawable/ic_tile_icon"
            android:exported="true"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
        <service
            android:name=".quicksettings.MachineThreeTileService"
            android:label="@string/tile_name_machine_three"
            android:icon="@drawable/ic_tile_icon"
            android:exported="true"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
    </application>

</manifest>